const state = {
  // For now, add lightTheme in currentTheme. Do replacing, etc. later
  currentTheme: {
    name: "Light Theme"
  },
  lightTheme: {},
  darkTheme: {},
  outlineTheme: {}
};

const getters = {};

const mutations = {};

const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
};
